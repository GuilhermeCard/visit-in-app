export type CheckinProgressModel = {
    id: string,
    name: string,
    lastCheckin: string,
    isAvailableToClaim: boolean,
    rewardsUnlocked: number,
    rewards: number,
    image: string,
};
